package src.server;

/**
 * Главный класс серверного приложения.
 * Данный класс является точкой входа (entry point) для запуска сервера.
 * Его единственная задача — передать управление классу ServerApp для
 * инициализации менеджеров и запуска сетевого взаимодействия.
 */
public class ServerMain {

    /**
     * Статический метод, с которого начинается выполнение серверной части программы.
     * Вызывает метод старта в ServerApp, который подготавливает окружение и
     * запускает бесконечный цикл ожидания клиентских подключений.
     *
     * @param args Аргументы командной строки, передаваемые при запуске (в текущей реализации не используются).
     */
    public static void main(String[] args){
        ServerApp.start(args);
    }
}